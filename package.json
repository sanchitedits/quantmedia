{
  "name": "html-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js"
  },
  "dependencies": {
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
